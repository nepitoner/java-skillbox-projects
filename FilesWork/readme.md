# A program that collects data from various sources

Learnt how to:
* traverse the folder tree recursively;
* read files in CSV and JSON formats, work with the data they contain;
* receive data from HTML pages;
* create JSON files.
